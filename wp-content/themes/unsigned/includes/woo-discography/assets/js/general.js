/*
jQuery(document).ready(function(){
	jQuery( '.woo-audio-player' ).each ( function () {
		var playerID = jQuery( this ).attr( 'id' );
		playerID = playerID.replace( 'player-', '' );
		
		jQuery( this ).ttwMusicPlayer( playlistNumber736, {
	        autoPlay:false, 
	        jPlayer:{
	            swfPath: woo_album_data.jplayer_path, 
	            solution: 'html', 'flash'
	        }
	    });
    });
});
*/